import * as React from 'react';
import {
	useRef
} from 'react';

import { AuthPluginUserEditComponent } from './authPluginUserEdit';

export const UserEditor: AuthPluginUserEditComponent = () =>
{
	return <p>
		Nothing to configure... after all, it's No Auth!
	</p>;
};
